countSumOfEvenNumbers(1,1000);
function countSumOfEvenNumbers(min,max) {
    let sum = 0;
    for (let i = min; index < max; index++) {
        isEvenNumber(i) ? sum += i: ;
    }
}

function isEvenNumber(number) {
    if (number % 2 == 0) {
        return true;
    }
    return false;
}